<%= simple_form_for [@instrument, @request] do |f| %>
<%= f.input :start_date, as: :date, html5: true%>
<%= f.input :end_date, as: :date, html5: true %>
<%= f.submit "Book #{@instrument.name}", class: "btn btn-primary" %>
<% end %>
